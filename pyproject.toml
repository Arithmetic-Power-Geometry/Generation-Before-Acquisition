[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "resolution-frontier-sort"
version = "0.1.0"
description = "Experimental resource-relative exact sorting"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
